{% extends 'base.html.twig' %}

{% block body %}
    <h1>Skill list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Niveau</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for skill in skills %}
            <tr>
                <td><a href="{{ path('admin_skills_show', { 'id': skill.id }) }}">{{ skill.id }}</a></td>
                <td>{{ skill.nom }}</td>
                <td>{{ skill.niveau }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_skills_show', { 'id': skill.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_skills_edit', { 'id': skill.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_skills_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
